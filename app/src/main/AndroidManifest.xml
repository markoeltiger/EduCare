<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.educare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SingUpAs"></activity>
        <activity android:name=".SignUpForProfessor" />
        <activity android:name=".professors_chat" />
        <activity android:name=".materials" />
        <activity android:name=".professors" />
        <activity android:name=".courses_activity" />
        <activity android:name=".chat_activity" />
        <activity android:name=".student_activity" />
        <activity android:name=".Sign_UpActivity" />
        <activity android:name=".DashBoard" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>